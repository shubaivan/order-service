framework:
    messenger:
        transports:
            product_sync:
                dsn: '%env(MESSENGER_TRANSPORT_DSN)%'
                options:
                    exchange:
                        name: products
                        type: fanout
                    queues:
                        order_service_product_sync: ~

        routing:
            Shared\Message\ProductSyncMessage: product_sync
